#!/usr/bin/env php
<?php

$again = true;
while (trim($again) !== 'n') {
    $who = readline('As a ');
    $do = readline('I want to ');
    $so = readline('So that I can ');

    $block = 'As a ' . $who . "\n";
    $block .= 'I want to ' . $do . "\n";
    $block .= 'So that I can ' . $so . "\n\n";

    file_put_contents('userstories', $block, FILE_APPEND);
    echo "\n";
    $again = readline('More? (Y/n)');
}
exit(0);
