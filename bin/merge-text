#!/bin/sh

CURRENT="$1"
ANCESTOR="$2"
OTHER="$3"

flip -u "$CURRENT"
flip -u "$ANCESTOR"
flip -u "$OTHER"
rtrim "$CURRENT"
rtrim "$ANCESTOR"
rtrim "$OTHER"

exec git merge-file "$CURRENT" "$ANCESTOR" "$OTHER"
